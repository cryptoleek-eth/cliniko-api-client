// Main entry point for the Cliniko TypeScript client
export { ClinikoClient } from './client';
export * from './types';

// Re-export for convenience
import { ClinikoClient } from './client';

export default ClinikoClient;